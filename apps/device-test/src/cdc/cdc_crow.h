#ifndef DEVICE_TEST_CDC_CROW_H
#define DEVICE_TEST_CDC_CROW_H

/*
 * crow (monome) CDC ACM — phase 1 is identify/log only via cdc_dev.
 *
 * future: command/response framing, poll timer, and focus UI would live
 * here once crow protocol helpers are added.
 *
 * identity: VID 0x0483 / PID 0x5740 (STM32 VCP used by crow firmware).
 */

#endif
